# Main Simulink Model

The repository is prepared for the following main model file:

```text
MMC_phase_hackathon_circuit4_HAAS_may1_localrank.slx
```

Expected location:

```text
MMC_models/MMC_phase_hackathon_circuit4_HAAS_may1_localrank.slx
```

The `.slx` model is a binary MATLAB/Simulink file. It should be uploaded to this folder directly through GitHub web upload, GitHub Desktop, or normal Git command-line upload.

## Recommended command-line upload

```bash
git clone https://github.com/zaidon87/zaidon87-MMC_phase_hackathon_circuit4_HAAS_may1_localrank.git
cd zaidon87-MMC_phase_hackathon_circuit4_HAAS_may1_localrank
mkdir -p MMC_models
cp /path/to/MMC_phase_hackathon_circuit4_HAAS_may1_localrank.slx MMC_models/
git add MMC_models/MMC_phase_hackathon_circuit4_HAAS_may1_localrank.slx
git commit -m "Add Simulink local-rank MMC model"
git push origin main
```

## Model integrity reference

SHA256 of the packaged model file:

```text
e1e6f55c12f46838b313b6d75a7079a1b6d8472a116c7ce58edd67db2e833332
```
