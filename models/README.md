# MATLAB / Simulink Models

The main Simulink model is stored in:

```text
MMC_models/MMC_phase_hackathon_circuit4_HAAS_may1_localrank.slx
```

This `models/` folder is kept for compatibility with earlier project templates.  
Use `MMC_models/` as the canonical location, following the referenced OwnTech/MMC repository style.
